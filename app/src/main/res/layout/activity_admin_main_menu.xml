<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_vertical"
    android:orientation="vertical"
    android:background="@drawable/woodbg"
    tools:context=".admin_activity.AdminMainMenu">

    <Button
        android:id="@+id/btn_adminAddPizza"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/shapes"
        android:textColor="@android:color/white"
        android:onClick="addAdminPizza"
        android:text="Manage Pizzas" />

    <Button
        android:id="@+id/btnManageVoucher"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/shapes"
        android:onClick="manageVouchers"
        android:text="Manage Vouchers"
        android:textColor="@android:color/white" />

</LinearLayout>